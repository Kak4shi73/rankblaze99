// Vercel Analytics Configuration
import { Analytics } from '@vercel/analytics/react';

// Export analytics component to be used in the app
export const AnalyticsComponent = () => {
  return <Analytics />;
};

export default AnalyticsComponent; 